# Local Development Environment Variables for Vennekredsen
POSTGRES_DB=vennekredsen_dev
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password

# JWT Secret for Development
JWT_SECRET=dev_jwt_secret_change_in_production

# Development token (not needed for local)
INTERNAL_TOKEN=dev_internal_token

# PgAdmin settings for development
PGADMIN_EMAIL=admin@dev.local
PGADMIN_PASSWORD=dev_admin